import styled from "@emotion/styled";
import { HeaderSize } from "src/styled";

export const PickSetStyled = styled.div`
  color: #000;
  margin-top: 40px;
  padding-top: ${HeaderSize}px;
`;

export const TitleStyled = styled.h2`
  text-align: center;
  font-size: 32px;
  text-transform: uppercase;
  font-weight: 700;
  margin-bottom: 40px;
`;
